         HOLLOW RECTANGLE

SOURCE CODE:

#include <stdio.h>

int main()
{  
    int len=10, bre=5;
    for(int i=0; i<bre; i++){
        for(int j=0; j<len; j++){
            if(i==0 || j==0 || i==bre-1 || j==len-1)
                printf("* ");
            else
                printf("  ");
        }
        printf("\n");
    }
}

OUTPUT:

* * * * * * * * * * 
*                 * 
*                 * 
*                 * 
* * * * * * * * * * 
